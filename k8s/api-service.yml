apiVersion: v1
kind: Service

metadata:
  name: cricbuzz-api-service

spec:
  type: NodePort

  selector:
    app: cricbuzz-api

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30007